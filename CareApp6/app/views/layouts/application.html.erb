<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Careapp Two</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body class = "container">

  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="/">Careapp Two</a>
        <div class="nav-collapse">
            <ul class="nav">
              <li><a href="/about">About</a></li>
              <li><a href="/contact">Contact</a></li>
                <% if session["user_id"].present? %>
            <!--      Hello, <%#= User.find_by_id(session["user_id"]).email %>! -->
  
              <li> <%= link_to 'Your Account', "/users/#{session["user_id"]}" %> </li>
              <li> <%= link_to "Sign Out", "/logout" %> </li>

                <% else %>
              <li>    <%= link_to "Sign Up", '/users/new' %> </li>
              <li> <%= link_to "Login", '/login' %> </li>
                <% end %>
            </ul>

        </div>
      </div>
    </div>
  </div>

  <div class="container">

    <% if flash[:notice] %>
    <div class="alert alert-success">
      <button type="button" class="close" data-dismiss="alert">&times;</button>
      <%= notice %>
    </div>
    <% end %>

    <%= yield %>

    <hr>
<!--
    <footer>
      <p>&copy; CareApp Two 2013</p>
    </footer>
-->

  </div>

</body>
</html>
